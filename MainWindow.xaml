<Window x:Class="wpf_lab3.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:wpf_lab3"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <!-- STYL I SZABLON: Okrągły przycisk z wyzwalaczem (Trigger) -->
        <Style x:Key="RoundButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#3498db"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Ellipse x:Name="circle" Fill="{TemplateBinding Background}" Stroke="Black" StrokeThickness="1"/>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="circle" Property="Fill" Value="#2980b9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <DockPanel>
        <StackPanel Margin="20">

            <!-- BINDOWANIE I KOMENDA (MVVM) -->
            <TextBlock Text="{Binding AppTitle}" FontSize="20" HorizontalAlignment="Center" Margin="0,0,0,20"/>

            <Button Content="Zmień tekst (MVVM)" 
                Command="{Binding ChangeTextCommand}" 
                Width="150" Height="40"/>

            <!-- OKRĄGŁY PRZYCISK ZE STYLEM -->
            <Button Style="{StaticResource RoundButtonStyle}" 
                Content="Styl" 
                Width="80" Height="80" Margin="0,20"/>

            <!-- ANIMACJA: Wyzwalana zdarzeniem Click -->
            <Rectangle x:Name="AnimatedRect" Fill="Green" Width="100" Height="30" HorizontalAlignment="Center">
                <Rectangle.Triggers>
                    <EventTrigger RoutedEvent="Rectangle.MouseDown">
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="Width"
                                             To="400" Duration="0:0:1" AutoReverse="True"/>
                                <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                             From="1" To="0.2" Duration="0:0:0.5" AutoReverse="True"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                </Rectangle.Triggers>
            </Rectangle>
            <TextBlock Text="Kliknij zielony prostokąt, żeby animować" HorizontalAlignment="Center" FontSize="10"/>

        </StackPanel>
        
        <Viewport3D x:Name="MyViewport">

            <!-- 1. KAMERA -->
            <Viewport3D.Camera>
                <PerspectiveCamera Position="0,0,5" LookDirection="0,0,-1" FieldOfView="45" />
            </Viewport3D.Camera>

            <!-- 2. MODELE I ŚWIATŁO -->
            <ModelVisual3D>
                <ModelVisual3D.Content>
                    <Model3DGroup>
                        <!-- Światło kierunkowe (jak słońce) -->
                        <DirectionalLight Color="White" Direction="-1,-1,-3" />

                        <!-- Nasz obiekt (Trójkąt) -->
                        <GeometryModel3D>
                            <GeometryModel3D.Geometry>
                                <!-- Positions: x,y,z wierzchołków -->
                                <MeshGeometry3D 
                                    Positions="-1,-1,0 1,-1,0 0,1,0" 
                                    TriangleIndices="0 1 2" />
                            </GeometryModel3D.Geometry>

                            <GeometryModel3D.Transform>
                                <RotateTransform3D>
                                    <RotateTransform3D.Rotation>
                                        <!-- Nazywamy rotację, aby dostać się do niej z C# -->
                                        <AxisAngleRotation3D x:Name="MyRotation" Axis="0,1,0" Angle="0" />
                                    </RotateTransform3D.Rotation>
                                </RotateTransform3D>
                            </GeometryModel3D.Transform>

                            <GeometryModel3D.Material>
                                <DiffuseMaterial Brush="Cyan" />
                            </GeometryModel3D.Material>
                        </GeometryModel3D>
                    </Model3DGroup>
                </ModelVisual3D.Content>
            </ModelVisual3D>

        </Viewport3D>
        <Button x:Name="AnimatedButton" 
        Content="Powiększ mnie!" 
        Width="150" 
        Height="50" 
        Click="AnimatedButton_Click" />
    </DockPanel>
    
</Window>
